apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: circleguard-dev
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
  selector:
    app: notification-service