apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-service-config
  namespace: circleguard-dev
data:
  DB_URL: "jdbc:postgresql://postgres:5432/circleguard_identity"
  DB_USERNAME: "admin"
  SERVER_PORT: "8083"
  CORS_ALLOWED_ORIGINS: "http://localhost:8081,http://localhost:8080"
