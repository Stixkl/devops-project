apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: circleguard-dev
spec:
  type: LoadBalancer
  ports:
  - port: 8087
    targetPort: 8087
  selector:
    app: gateway-service