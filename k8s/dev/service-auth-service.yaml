apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: circleguard-dev
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 8180
    targetPort: 8180
    protocol: TCP
  selector:
    app: auth-service