apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: circleguard-dev
  labels:
    app: dashboard-service
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
  selector:
    app: dashboard-service
