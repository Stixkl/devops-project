apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: circleguard-dev
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
  selector:
    app: dashboard-service
