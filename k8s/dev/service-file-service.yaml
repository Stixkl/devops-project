apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: circleguard-dev
  labels:
    app: file-service
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
  selector:
    app: file-service
